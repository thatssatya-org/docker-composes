name: Deploy immich processing

on:
  push:
    branches:
      - main
    paths:
      - 'immich/immich-processing.yml'
      - 'immich/hwaccel.transcoding.yml'
      - 'immich/hwaccel.ml.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy-compose.yml
    with:
      ssh_user: ${{ vars.IMMICH_ML_USER }}
      ssh_host: ${{ vars.IMMICH_ML_HOST }}
      app_directory: immich
      docker_compose_file: immich-processing.yml
    secrets:
      tailscale_authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
      env_file_contents: ${{ secrets.IMMICH_PROCESSING_DOT_ENV_SECRETS }}